<div class="centered-form">
  <%= form_with(model: @map) do |f| %>
    <div class="form-group">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>

    <%= f.submit 'Create Map', class: 'form-submit' %>
  <% end %>
</div>
